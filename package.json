{
  "name": "tabolds",
  "version": "1.0.0",
  "description": "tabnews.com.br clone",
  "main": "index.js",
  "scripts": {
    "dev": "next dev",
    "lint:check": "prettier --check .",
    "lint:fix": "prettier --write .",
    "test": "jest",
    "test:watch": "jest --watchAll",
    "dale:up": "docker compose --env-file .env -f infra/compose.yml up -d",
    "dale:down": "docker compose --env-file .env -f infra/compose.yml down"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "next": "^13.1.6",
    "pg": "^8.11.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "jest": "^29.6.2",
    "prettier": "^3.0.1"
  }
}
